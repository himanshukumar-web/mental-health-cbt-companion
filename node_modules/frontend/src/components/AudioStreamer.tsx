"use client";

export default function AudioStreamer() {
  // Voice input is scaffolded for future implementation.
  // Phase 2 will wire WebRTC microphone capture → WebSocket binary frames
  // → server-side Whisper transcription → chat pipeline.
  return null;
}
