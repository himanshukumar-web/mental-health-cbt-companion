"use client";

import { useState } from "react";
import { GAD7Assessment } from "@/types/persona";
import { motion, AnimatePresence } from "framer-motion";
import toast from "react-hot-toast";

const GAD7_QUESTIONS = [
  "Feeling nervous, anxious, or on edge",
  "Not being able to stop or control worrying",
  "Worrying too much about different things",
  "Trouble relaxing",
  "Being so restless that it is hard to sit still",
  "Becoming easily annoyed or irritable",
  "Feeling afraid as if something awful might happen",
];

const OPTIONS = [
  { label: "Not at all", value: 0 },
  { label: "Several days", value: 1 },
  { label: "More than half the days", value: 2 },
  { label: "Nearly every day", value: 3 },
];

interface GAD7WizardProps {
  onSubmit: (answers: number[]) => Promise<GAD7Assessment | null>;
  latestAssessment?: GAD7Assessment | null;
}

export default function GAD7Wizard({ onSubmit, latestAssessment }: GAD7WizardProps) {
  const [step, setStep] = useState<number>(0);
  const [answers, setAnswers] = useState<number[]>(Array(7).fill(-1));
  const [result, setResult] = useState<GAD7Assessment | null>(latestAssessment || null);
  const [submitting, setSubmitting] = useState<boolean>(false);

  const handleSelect = (val: number) => {
    const next = [...answers];
    next[step] = val;
    setAnswers(next);

    if (step < 6) {
      setStep(step + 1);
    }
  };

  const handleSubmit = async () => {
    if (answers.some((a) => a === -1)) {
      toast.error("Please answer all 7 questions.");
      return;
    }
    setSubmitting(true);
    try {
      const res = await onSubmit(answers);
      if (res) {
        setResult(res);
        toast.success("GAD-7 Assessment Completed!");
      }
    } catch {
      toast.error("Failed to submit assessment.");
    } finally {
      setSubmitting(false);
    }
  };

  const resetWizard = () => {
    setAnswers(Array(7).fill(-1));
    setStep(0);
    setResult(null);
  };

  if (result) {
    return (
      <motion.div
        initial={{ opacity: 0, y: 12 }}
        animate={{ opacity: 1, y: 0 }}
        style={{
          padding: 24,
          borderRadius: 20,
          background: "var(--bg-glass)",
          backdropFilter: "blur(16px)",
          border: "1px solid var(--border-secondary)",
          display: "flex",
          flexDirection: "column",
          gap: 16,
        }}
      >
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
          <div>
            <span style={{ fontSize: 12, fontWeight: 700, textTransform: "uppercase", color: "#8b5cf6", letterSpacing: "0.08em" }}>
              Clinical Anxiety Assessment
            </span>
            <h2 style={{ fontSize: 22, fontWeight: 800, color: "var(--text-primary)", margin: "4px 0 0" }}>
              GAD-7 Anxiety Scale
            </h2>
          </div>
          <button
            onClick={resetWizard}
            style={{
              padding: "8px 14px",
              borderRadius: 10,
              border: "1px solid var(--border-secondary)",
              background: "var(--bg-secondary)",
              color: "var(--text-primary)",
              fontSize: 12,
              fontWeight: 600,
              cursor: "pointer",
            }}
          >
            Retake GAD-7
          </button>
        </div>

        <div
          style={{
            display: "grid",
            gridTemplateColumns: "repeat(auto-fit, minmax(180px, 1fr))",
            gap: 16,
            marginTop: 8,
          }}
        >
          <div
            style={{
              padding: 16,
              borderRadius: 14,
              background: "rgba(139,92,246,0.08)",
              border: "1px solid rgba(139,92,246,0.2)",
              textAlign: "center",
            }}
          >
            <div style={{ fontSize: 11, color: "var(--text-tertiary)", fontWeight: 600, textTransform: "uppercase" }}>
              Anxiety Score
            </div>
            <div style={{ fontSize: 36, fontWeight: 900, color: "#8b5cf6", marginTop: 4 }}>
              {result.score} <span style={{ fontSize: 16, color: "var(--text-tertiary)", fontWeight: 600 }}>/ 21</span>
            </div>
          </div>

          <div
            style={{
              padding: 16,
              borderRadius: 14,
              background: "rgba(6,182,212,0.08)",
              border: "1px solid rgba(6,182,212,0.2)",
              textAlign: "center",
            }}
          >
            <div style={{ fontSize: 11, color: "var(--text-tertiary)", fontWeight: 600, textTransform: "uppercase" }}>
              Anxiety Level
            </div>
            <div style={{ fontSize: 20, fontWeight: 800, color: "#06b6d4", marginTop: 8 }}>
              {result.anxiety_level}
            </div>
          </div>
        </div>

        <div style={{ padding: 16, borderRadius: 14, background: "var(--bg-secondary)", border: "1px solid var(--border-secondary)" }}>
          <h4 style={{ fontSize: 14, fontWeight: 700, color: "var(--text-primary)", margin: "0 0 6px" }}>
            🧠 AI Anxiety Analysis & Coping Recommendations
          </h4>
          <p style={{ fontSize: 13, color: "var(--text-secondary)", lineHeight: 1.5, margin: 0 }}>
            {result.ai_explanation}
          </p>
        </div>
      </motion.div>
    );
  }

  return (
    <div
      style={{
        padding: 24,
        borderRadius: 20,
        background: "var(--bg-glass)",
        backdropFilter: "blur(16px)",
        border: "1px solid var(--border-secondary)",
      }}
    >
      <div style={{ marginBottom: 20 }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 8 }}>
          <span style={{ fontSize: 12, fontWeight: 700, color: "#8b5cf6", textTransform: "uppercase" }}>
            GAD-7 Question {step + 1} of 7
          </span>
          <span style={{ fontSize: 12, color: "var(--text-tertiary)", fontWeight: 600 }}>
            {Math.round(((step + 1) / 7) * 100)}% Complete
          </span>
        </div>

        <div style={{ height: 6, borderRadius: 3, background: "var(--bg-secondary)", overflow: "hidden" }}>
          <motion.div
            initial={{ width: 0 }}
            animate={{ width: `${((step + 1) / 7) * 100}%` }}
            style={{ height: "100%", background: "linear-gradient(90deg, #8b5cf6, #c084fc)", borderRadius: 3 }}
          />
        </div>
      </div>

      {/* Question prompt */}
      <div style={{ minHeight: "auto", margin: "16px 0", display: "flex", flexDirection: "column", justifyContent: "center" }}>
        <div style={{ fontSize: 12, color: "var(--text-tertiary)", marginBottom: 6 }}>
          Over the last 2 weeks, how often have you been bothered by:
        </div>
        <AnimatePresence mode="wait">
          <motion.h3
            key={step}
            initial={{ opacity: 0, x: 20 }}
            animate={{ opacity: 1, x: 0 }}
            exit={{ opacity: 0, x: -20 }}
            transition={{ duration: 0.2 }}
            style={{ fontSize: 16, fontWeight: 700, color: "var(--text-primary)", lineHeight: 1.4, margin: "0 0 16px" }}
          >
            {GAD7_QUESTIONS[step]}
          </motion.h3>
        </AnimatePresence>

        <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
          {OPTIONS.map((opt) => {
            const isSelected = answers[step] === opt.value;
            return (
              <motion.button
                key={opt.value}
                whileHover={{ scale: 1.01 }}
                whileTap={{ scale: 0.99 }}
                onClick={() => handleSelect(opt.value)}
                style={{
                  padding: "10px 14px",
                  borderRadius: 12,
                  border: isSelected ? "2px solid #8b5cf6" : "1px solid var(--border-secondary)",
                  background: isSelected ? "rgba(139,92,246,0.12)" : "var(--bg-secondary)",
                  color: isSelected ? "#8b5cf6" : "var(--text-primary)",
                  fontSize: 13,
                  fontWeight: isSelected ? 700 : 500,
                  cursor: "pointer",
                  textAlign: "left",
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "space-between",
                  transition: "all 0.15s",
                }}
              >
                <span>{opt.label}</span>
                <span style={{ fontSize: 11, opacity: 0.6 }}>+{opt.value} pts</span>
              </motion.button>
            );
          })}
        </div>
      </div>

      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginTop: 24 }}>
        <button
          disabled={step === 0}
          onClick={() => setStep(step - 1)}
          style={{
            padding: "8px 16px",
            borderRadius: 10,
            border: "1px solid var(--border-secondary)",
            background: "transparent",
            color: "var(--text-secondary)",
            fontSize: 13,
            fontWeight: 600,
            cursor: step === 0 ? "not-allowed" : "pointer",
            opacity: step === 0 ? 0.4 : 1,
          }}
        >
          ← Previous
        </button>

        {step === 6 && answers.every((a) => a !== -1) && (
          <motion.button
            whileHover={{ scale: 1.03 }}
            whileTap={{ scale: 0.97 }}
            onClick={handleSubmit}
            disabled={submitting}
            style={{
              padding: "10px 24px",
              borderRadius: 12,
              border: "none",
              background: "linear-gradient(135deg, #8b5cf6, #6d28d9)",
              color: "#fff",
              fontSize: 14,
              fontWeight: 700,
              cursor: "pointer",
              boxShadow: "0 4px 16px rgba(139,92,246,0.3)",
            }}
          >
            {submitting ? "Analyzing..." : "Submit GAD-7 Assessment ✨"}
          </motion.button>
        )}
      </div>
    </div>
  );
}
